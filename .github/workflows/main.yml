name: first-action
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
    - name: Print greeting
      run: echo "hello, world!"
    - name: Print farewell
      run: echo "goodbye!"
  second-job:
    runs-on: ubuntu-latest
    steps:
      name: Print node version
      run: node -version